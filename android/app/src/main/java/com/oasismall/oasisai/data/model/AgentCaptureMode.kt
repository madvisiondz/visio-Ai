package com.oasismall.oasisai.data.model

enum class AgentCaptureMode {
    /** Full PARAY, lock, designation, alternates — current workflow. */
    SMART,
    /** Mall bulk shoot: barcode only, replace or skip, bulk_captures DB. */
    BULK,
}
