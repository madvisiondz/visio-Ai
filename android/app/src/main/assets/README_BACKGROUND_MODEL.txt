Offline background removal model (bundled in release APK):

  u2netp.onnx  (~4.4 MB, U2NetP via ONNX Runtime)

To refresh the model file on a dev machine:
  .\scripts\download-u2netp-tflite.ps1

Then rebuild. The phone never needs internet for cutout.
